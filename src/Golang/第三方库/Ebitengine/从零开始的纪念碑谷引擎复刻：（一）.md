---
title: 从零开始的纪念碑谷引擎复刻：（一）
date: 2025-12-30
---
[[toc]]
## 前言：何为Ebitengine
### 超简单
> In Ebitengine, everything is an image: the screen, data from an image file, and even offscreen items are all represented as image objects. Most rendering operations consist of drawing one image on top of another.

在Ebitengine中，一切都只是一张图片：不论是图像文件中的画面和数据，亦或是不在画面中的物品，都以图片数据格式组织。大多数渲染操作都只是在一张图片上绘制另一张图片
### 跨平台
> Ebitengine games work on desktop (Windows, macOS, Linux, and FreeBSD), web browsers (through WebAssembly), and even on mobile (Android and iOS)! Plus, Ebitengine is implemented in pure Go on Windows, so Windows developers do not need to install a C compiler. Nintendo Switch™ is also supported!

Ebitengine游戏支持桌面端（Windows、MacOS、Linux和FreeBSD），浏览器（通过WASM）和手机端（安卓和苹果）。此外，由于Ebitengine完全由Go编写，所以Windows开发者不需要额外安装C语言编译器。任天堂也是支持的噢！

### 高性能
> While Ebitengine's drawing API is very simple, Ebitengine games run very fast with GPU power. Multiple images are integrated into a texture atlas internally, and drawing operations are automatically performed in batch when possible.

Ebitengine不仅有着超简单的图形绘制API，还能借助GPU的算力跑得飞快。引擎内部会将多张图像整合成一张，并尽可能地自动批量绘制

### 蓄势待发
> Ebitengine has been used to develop production-level games. One such example is [Bear's Restaurant](https://daigostudio.com/bearsrestaurant/), a mobile application that has been downloaded over 1,000,000 times.

Ebitengine已被用于开发生产级别的游戏。比如[棕熊餐厅](https://daigostudio.com/bearsrestaurant/)这款下载量已达一百万次的移动端游戏

## Hello Rhombus（菱形）
### 等距投影
纪念碑谷所使用的投影方式是**正交投影**中的**等距投影**，而Ebitengine是2D引擎。这里需要从三维逻辑坐标（以`z`作为逻辑深度）映射至二维坐标
![](assets/PixPin_2025-12-30_15-10-00.mp4)

$$
令逻辑坐标为(x, y , depth), 屏幕坐标为(X, Y); 固定等距投影倾角为θ, 有
$$$$
X = (x - z) cos(θ) ; \newline
Y = (x + z) sin(θ) + y
$$
*等距投影标准倾角为: 30°*

![](assets/Pasted%20image%2020251230152106.png)
进一步绘制基准点
$X_{axis} = (cos(theta) \cdot t, sin(theta) * t)$
$Z_{axis} = (-cos(theta) * t, sin(theta) * t)$
$Y_{axis} = (0, t), t为参数变量$

![](assets/PixPin_2025-12-30_15-34-32.mp4)
*30度倾角下（0， 5， 0）等距投影后等效于(5, 0, 5)*

### 等距投影-矩阵运算
![](assets/Pasted%20image%2020251230155341.png)
$$
M = \begin{bmatrix}
cos(alpha) & 0 & -cos(alpha) \\\\
sin(alpha) & 1 & sin(alpha) 
\end{bmatrix}
$$
$V$是一个三维坐标：$(x, y, z)$
所以$V \cdot M$即为：
$$
\begin{bmatrix}
x \\\\
y \\\\
z
\end{bmatrix}
\cdot
\begin{bmatrix}
cos(alpha) & 0 & -cos(alpha) \\\\
sin(alpha) & 1 & sin(alpha) 
\end{bmatrix}
=
\begin{bmatrix}
x\cdot cos(alpha) & 0 & -z\cdot cos(alpha) \\\\
x\cdot sin(alpha) & y & -z\cdot sin(alpha) 
\end{bmatrix}
$$
恰好对应上之前的形式![](assets/Pasted%20image%2020251230160003.png)

### 绘制立方体
![](assets/Pasted%20image%2020251230161250.png)
以刚才的点V为几何中心，以`0.5`为单位偏移距离，一点一点画出八个顶点；以实线为看得见的线，虚线为被遮挡的线

![](assets/PixPin_2025-12-30_16-22-44.mp4)

***
# 页面底部